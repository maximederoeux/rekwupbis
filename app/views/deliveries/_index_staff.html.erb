0
<div class="row">
<ul class="list-group">
	        <li class="list-group-item list-group-item-warning ">
	          <h4><%= t("delivery.prepare") %></h4>
	        </li>
</ul>
</div>

  <div class="row">
    <div class="col-md-6">    
    	<div class="col-md-6">
    		<ul class="list-group">
	        <li class="list-group-item text-center">
	          <h4><%= t("delivery.today") %></h4>
	        </li>
  			
  					<% @not_ready.this_day.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>

				          <% if delivery.offer.lln_daily %>

							      <%= simple_form_for(delivery) do |f| %>
							        <%= f.error_notification %>
							        <%= f.hidden_field :is_ready, :value => true %>
							        <%= f.hidden_field :ready_time, :value => Time.now %>
							        <%= f.hidden_field :ready_by, :value => current_user.id %>
							        <%= f.submit t("delivery.thisready"), class: "btn btn-block btn-info" %>
							      <% end %>

							     <% end %>



								</li>

							<% end %>
	        	<% end %>
	     	</ul>
	    </div>

			<div class="col-md-6">
				<ul class="list-group">
	        <li class="list-group-item text-center">
	          <h4><%= t("delivery.tomorrow") %></h4>
	        </li>

  					<% @not_ready.next_day.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				                        
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %>

				          <% if delivery.offer.lln_daily %>
				          
							      <%= simple_form_for(delivery) do |f| %>
							        <%= f.error_notification %>
							        <%= f.hidden_field :is_ready, :value => true %>
							        <%= f.hidden_field :ready_time, :value => Time.now %>
							        <%= f.hidden_field :ready_by, :value => current_user.id %>
							        <%= f.submit t("delivery.thisready"), class: "btn btn-block btn-info" %>
							      <% end %>

							     <% end %>



				          </strong>
								</li>
							<% end %>
	        	<% end %>	      
     		</ul>
     	</div>
      
    </div>  

        
    <div class="col-md-6">
    	<div class="col-md-6">
    		<ul class="list-group">
	        <li class="list-group-item text-center">
	          <h4><%= t("delivery.two_days") %></h4>
	        </li>
  			
  					<% @not_ready.two_days.each do |delivery| %>

  					  <% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            			            
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %>

				          <% if delivery.offer.lln_daily %>
				          
							      <%= simple_form_for(delivery) do |f| %>
							        <%= f.error_notification %>
							        <%= f.hidden_field :is_ready, :value => true %>
							        <%= f.hidden_field :ready_time, :value => Time.now %>
							        <%= f.hidden_field :ready_by, :value => current_user.id %>
							        <%= f.submit t("delivery.thisready"), class: "btn btn-block btn-info" %>
							      <% end %>

							     <% end %>



				          </strong>
								</li>
							<% end %>
        		<% end %>
	     	</ul>
	    </div>

			<div class="col-md-6">
				<ul class="list-group">
	        <li class="list-group-item text-center">
	          <h4><%= t("delivery.three_days") %></h4>
	        </li>

  					<% @not_ready.three_days.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
								<li class="list-group-item text-center">
	      	
				          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            		 
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %>

				          <% if delivery.offer.lln_daily %>
				          
							      <%= simple_form_for(delivery) do |f| %>
							        <%= f.error_notification %>
							        <%= f.hidden_field :is_ready, :value => true %>
							        <%= f.hidden_field :ready_time, :value => Time.now %>
							        <%= f.hidden_field :ready_by, :value => current_user.id %>
							        <%= f.submit t("delivery.thisready"), class: "btn btn-block btn-info" %>
							      <% end %>

							     <% end %>


				          </strong>
								</li>
							<% end %>
	        	<% end %>	      
     		</ul>
     	</div>
		</div>
	</div>

<div class="row">
	<ul class="list-group">
		        <li class="list-group-item list-group-item-warning ">
		          <h4><%= t("delivery.ready") %></h4>
		        </li>
	</ul>
	</div>

	  <div class="row">
	    <div class="col-md-6">    
	    	<div class="col-md-6">
	    		<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.today") %></h4>
		        </li>
	  			
	  					<% @not_gone.this_day.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
									<li class="list-group-item text-center">
		      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>

				          <% if delivery.offer.lln_daily %>

						        <%= simple_form_for(delivery) do |f| %>
						          <%= f.error_notification %>
						          <%= f.hidden_field :is_gone, :value => true %>
						          <%= f.hidden_field :gone_time, :value => Time.now %>
						          <%= f.hidden_field :sent_by, :value => current_user.id %>
						          <%= f.submit t("delivery.send"), class: "btn btn-block btn-info" %>
						        <% end %>

									<% end %>

									</li>
								<% end %>
		        	<% end %>
		     	</ul>
		    </div>

				<div class="col-md-6">
					<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.tomorrow") %></h4>
		        </li>

	  					<% @not_gone.next_day.each do |delivery| %>

	  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
									<li class="list-group-item text-center">
		      	
					          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            		
						            <%= link_to delivery_path(delivery) do %>
						              <i class="fa fa-shopping-cart fa-3x"></i>
						            <% end %>
					            <br>
					            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
					            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
					          </strong>
									</li>
								<% end %>
		        	<% end %>	      
	     		</ul>
	     	</div>
	      
	    </div>  

	        
	    <div class="col-md-6">
	    	<div class="col-md-6">
	    		<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.two_days") %></h4>
		        </li>
	  			
	  					<% @not_gone.two_days.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
							<% end %>
	        	<% end %>
		     	</ul>
		    </div>

				<div class="col-md-6">
					<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.three_days") %></h4>
		        </li>

	  					<% @not_gone.three_days.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
		        	<% end %>
		        <% end %>   
	     		</ul>
	     	</div>
			</div>
		</div>

<div class="row">
	<ul class="list-group">
		        <li class="list-group-item list-group-item-warning ">
		          <h4><%= t("delivery.gone") %></h4>
		        </li>
	</ul>
	</div>

	  <div class="row">
	    <div class="col-md-6">    
	    	<div class="col-md-6">
	    		<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.gone_today") %></h4>
		        </li>
	  			
	  					<% @gone.this_day.each do |delivery| %>

  							<% if delivery.offer_boxes.sum("quantity") >= 1 %>
									<li class="list-group-item text-center">

				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
									</li>
								<% end %>
		        	<% end %>
		     	</ul>
		    </div>

				<div class="col-md-6">
					<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.gone_yesterday") %></h4>
		        </li>

	  					<% @gone.previous_day.each do |delivery| %>

	  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
									<li class="list-group-item text-center">
		      	
					          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            		
						            <%= link_to delivery_path(delivery) do %>
						              <i class="fa fa-shopping-cart fa-3x"></i>
						            <% end %>
					            <br>
					            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
					            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
					          </strong>
									</li>
								<% end %>
		        	<% end %>	      
	     		</ul>
	     	</div>
	      
	    </div>  

	        
	    <div class="col-md-6">
	    	<div class="col-md-6">
	    		<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.gone_two_days") %></h4>
		        </li>
	  			
	  					<% @gone.two_days_ago.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
							<% end %>
	        	<% end %>
		     	</ul>
		    </div>

				<div class="col-md-6">
					<ul class="list-group">
		        <li class="list-group-item text-center">
		          <h4><%= t("delivery.gone_three_days") %></h4>
		        </li>

	  					<% @gone.three_days_ago.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
		        	<% end %>
		        <% end %>	      
	     		</ul>
	     	</div>
			</div>
		</div>

					<ul class="list-group">
		        <li class="list-group-item list-group-item-info text-center">
		          <h4><%= t("delivery.next_two_weeks") %></h4>
		        </li>

	  					<% @deliveries.next_two_weeks.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>
								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
		        	<% end %>
		        <% end %>    
	     		</ul>

					<ul class="list-group">
		        <li class="list-group-item list-group-item-info text-center">
		          <h4><%= t("delivery.one_year") %></h4>
		        </li>

	  					<% @deliveries.next_year.each do |delivery| %>

  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

								<li class="list-group-item text-center">
	      	
				          <strong>
			            <%= delivery.offer.event.event_name %><br>
				            <% if delivery.offer.organizer.is_lln %>
				            	<%= delivery.offer.organizer.name %><br>
				            <% end %>			            
			            		
					            <%= link_to delivery_path(delivery) do %>
					              <i class="fa fa-shopping-cart fa-3x"></i>
					            <% end %>
				            <br>
				            <%= delivery.delivery_date.strftime("%d/%m/%Y") %><br>
				            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
				          </strong>
								</li>
		        	<% end %>
		        <% end %>    
	     		</ul>

					<ul class="list-group">
		        <li class="list-group-item list-group-item-info text-center">
		          <h4>All of them</h4>
		        </li>

	  					<% @deliveries.each do |delivery| %>

	  						<% if delivery.offer_boxes.sum("quantity") >= 1 %>

									<li class="list-group-item text-center">
		      	
					          <strong>
				            <%= delivery.offer.event.event_name %><br>
					            <% if delivery.offer.organizer.is_lln %>
					            	<%= delivery.offer.organizer.name %><br>
					            <% end %>			            
				            		
						            <%= link_to delivery_path(delivery) do %>
						              <i class="fa fa-shopping-cart fa-3x"></i>
						            <% end %>
					            <br>
					            <%= delivery.delivery_date.strftime("%d/%m/%Y") if delivery.delivery_date %><br>
					            <%= delivery.total_boxes %> <%= t("menu.boxes") %> 
					          </strong>
									</li>
								<% end %>
		        	<% end %>   
	     		</ul>

