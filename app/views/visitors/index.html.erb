<div class="container-fluid more-padding">

  <% if user_signed_in? %>

    <% if @user.name.blank? %>

      <h4>Vous devez compléter votre profil avant de créer un événement.</h4>

      <%= link_to t("user.your_profile"), edit_user_path(@user), class: "btn btn-default" %>

    <% else %>

    Nous livrons les gobelets avant votre événement.
    Nous les reprennons sales après.
    Nous les nettoyons pour vous et ne vous facturons que le nettoyage, les gobelets cassés ou abimés et le transport.
<h4><%= t("article.our_prices") %></h4>
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th><%= t("article.name") %></th>
            <th><%= t("article.washing_price") %></th>
            <th><%= t("article.handwash_price") %></th>
            <th><%= t("article.tab_price") %></th>
            <th><strong><%= t("article.deposit_price") %></th>
          </tr>
        </thead>
    
      <% @rekwup_cups.each do |article| %>

        <tbody>
          <tr>
          <td><%= article.article_category %> <%= article.full_name %></td>
          <td><%= article.washing_price %> €</td>
          <td><%= article.handwash_price %> €</td>
          <td>
            <% if article.tab_price.present? %>
              <%= article.tab_price %> €
            <% end %>
          </td>
          <td><%= article.deposit_price %> €</td>
          </tr>
        </tbody>

      <% end %>
    </table>
  </div>





        


    <% end %>
 
  <% else %>

    Bienvenue chez Rekwup!








  <% end %>
</div>
<br>




