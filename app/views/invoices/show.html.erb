<div class="invoice-background">

<% if @invoice.doc_invoice && @invoice.after_event %>

	<%= render 'after_event_pdf' %>

<% end %>

<% if @invoice.doc_invoice && @invoice.confirmation %>

	<%= render 'deposit_pdf' %>

<% end %>

<% if @invoice.offer.lln_invoice %>

	<%= render 'lln_pdf' %>

<% end %>


</div>