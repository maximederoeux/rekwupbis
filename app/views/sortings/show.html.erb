<div class="container-fluid more-padding">


<% if @sorting.return_box.washes.where.not(:start_time => nil).count == 0 %>

  <h4><%= t("wash.no_wash") %></h4>

<% else %>

	<%= render 'show_general_info' %>

	<hr>

	<%= render 'show_clean' %>
	<%= render 'show_broken' %>
	<%= render 'show_very_dirty' %>
	<%= render 'show_handling' %>
	<hr>
	<%= render 'show_summary' %>






<% end %>

</div>