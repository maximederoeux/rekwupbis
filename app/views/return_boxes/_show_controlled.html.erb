  <ul class="list-group">
    <li class="list-group-item">

      <strong>
        <%= t("return.reference") %><%= @return_box.id %> <%= t("word.from") %> <%= @return_box.offer.event.event_name %><br>
                    <% if @return_box.delivery.offer.organizer.is_lln %>
                      <%= @return_box.delivery.offer.organizer.name %><br>
                    <% end %>    
      </strong>
    </li>

    <li class="list-group-item list-group-item-warning">

      <h5><strong>
        <%= t("return.is_controlled") %><br>   
      </strong></h5>
    </li>

      <li class="list-group-item">

          <strong><%= @return_box.display_difference %></strong>

      </li>

      <li class="list-group-item">

          <% @thisreturndetails.each do |detail| %>

            <% unless detail.dirty_ctrl_value - detail.dirty_value == 0 %>

              <!-- Large modal -->
              <button type="button" class="btn btn-lg btn-default" data-toggle="modal" data-target="#return_detail_<%= detail.id %>">
                <%= detail.box.box_name %><br>
                <%= t("return.dirty") %>

              </button>

              <div class="modal fade" id="return_detail_<%= detail.id %>" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
                <div class="modal-dialog modal-lg">
                  <div class="modal-content">

                    <%= simple_form_for detail do |f| %>

                      <div class="modal-header">

                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span>
                        </button>

                          <h4 class="modal-title" id="myModalLabel">

                            <div class="row text-center">

                              <div class="col-md-4">
                                 <%= t("box.box_type") %>
                              </div>

                              <div class="col-md-6">
                                 <%= f.collection_select :box_id, Box.all.order('box_name ASC'), :id, :box_name, {prompt: false}, {class: "form-control styled-select text-center"} %>
                              </div>

                              <div class="col-md-2">
                              </div>

                            </div>                     
                           
                          </h4>
                      </div>

                      <div class="modal-body">
                        <div class="row text-center">

                          <div class="col-md-6">
                            <%= t("return.return") %><br>
                            <%= f.input :dirty %>
                          </div>

                          <div class="col-md-6">
                            <%= t("return.control") %><br>
                            <%= f.input :dirty_ctrl %>
                          </div>

                        </div>
                      </div>

                      <div class="modal-footer">

                        <button type="button" class="btn btn-default" data-dismiss="modal">
                          <%= t("menu.close") %>
                        </button>
                
                          <%= f.button :submit, class: "btn btn-info" %>
          
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>

            <% unless detail.sealed_ctrl_value - detail.sealed_value == 0 %>

              <!-- Large modal -->
              <button type="button" class="btn btn-lg btn-default" data-toggle="modal" data-target="#return_detail_<%= detail.id %>">
                <%= detail.box.box_name %><br>
                <%= t("return.sealed") %>

              </button>

              <div class="modal fade" id="return_detail_<%= detail.id %>" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
                <div class="modal-dialog modal-lg">
                  <div class="modal-content">

                    <%= simple_form_for detail do |f| %>

                      <div class="modal-header">

                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span>
                        </button>

                          <h4 class="modal-title" id="myModalLabel">

                            <div class="row text-center">

                              <div class="col-md-4">
                                 <%= t("box.box_type") %>
                              </div>

                              <div class="col-md-6">
                                 <%= f.collection_select :box_id, Box.all.order('box_name ASC'), :id, :box_name, {prompt: false}, {class: "form-control styled-select text-center"} %>
                              </div>

                              <div class="col-md-2">
                              </div>

                            </div>                     
                           
                          </h4>
                      </div>

                      <div class="modal-body">
                        <div class="row text-center">

                          <div class="col-md-6">
                            <%= t("return.return") %><br>
                            <%= f.input :sealed %>
                          </div>

                          <div class="col-md-6">
                            <%= t("return.control") %><br>
                            <%= f.input :sealed_ctrl %>
                          </div>

                        </div>
                      </div>

                      <div class="modal-footer">

                        <button type="button" class="btn btn-default" data-dismiss="modal">
                          <%= t("menu.close") %>
                        </button>
                
                          <%= f.button :submit, class: "btn btn-info" %>
          
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>

            <% unless detail.clean_ctrl_value - detail.clean_value == 0 %>

              <!-- Large modal -->
              <button type="button" class="btn btn-lg btn-default" data-toggle="modal" data-target="#return_detail_<%= detail.id %>">
                <%= detail.box.box_name %><br>
                <%= t("return.sealed") %>

              </button>

              <div class="modal fade" id="return_detail_<%= detail.id %>" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
                <div class="modal-dialog modal-lg">
                  <div class="modal-content">

                    <%= simple_form_for detail do |f| %>

                      <div class="modal-header">

                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span>
                        </button>

                          <h4 class="modal-title" id="myModalLabel">

                            <div class="row text-center">

                              <div class="col-md-4">
                                 <%= t("box.box_type") %>
                              </div>

                              <div class="col-md-6">
                                 <%= f.collection_select :box_id, Box.all.order('box_name ASC'), :id, :box_name, {prompt: false}, {class: "form-control styled-select text-center"} %>
                              </div>

                              <div class="col-md-2">
                              </div>

                            </div>                     
                           
                          </h4>
                      </div>

                      <div class="modal-body">
                        <div class="row text-center">

                          <div class="col-md-6">
                            <%= t("return.return") %><br>
                            <%= f.input :clean %>
                          </div>

                          <div class="col-md-6">
                            <%= t("return.control") %><br>
                            <%= f.input :clean_ctrl %>
                          </div>

                        </div>
                      </div>

                      <div class="modal-footer">

                        <button type="button" class="btn btn-default" data-dismiss="modal">
                          <%= t("menu.close") %>
                        </button>
                
                          <%= f.button :submit, class: "btn btn-info" %>
          
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>

            <% unless @return_box.difference == 0 %>

              <!-- Large modal -->
              <button type="button" class="btn btn-lg btn-default" data-toggle="modal" data-target="#return_detail_<%= detail.id %>">
                <%= detail.box.box_name %><br>


              </button>

              <div class="modal fade" id="return_detail_<%= detail.id %>" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
                <div class="modal-dialog modal-lg">
                  <div class="modal-content">

                    <%= simple_form_for detail do |f| %>

                      <div class="modal-header">

                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span>
                        </button>

                          <h4 class="modal-title" id="myModalLabel">

                            <div class="row text-center">

                              <div class="col-md-4">
                                 <%= t("box.box_type") %>
                              </div>

                              <div class="col-md-6">
                                 <%= f.collection_select :box_id, Box.all.order('box_name ASC'), :id, :box_name, {prompt: false}, {class: "form-control styled-select text-center"} %>
                              </div>

                              <div class="col-md-2">
                              </div>

                            </div>                     
                           
                          </h4>
                      </div>

                      <div class="modal-body">
                        <div class="row text-center">

                          <div class="col-md-4">
                            <%= t("return.return") %><br>
                            <%= f.input :dirty %>
                          </div>

                          <div class="col-md-4">
                            <%= t("return.return") %><br>
                            <%= f.input :sealed %>
                          </div>

                          <div class="col-md-4">
                            <%= t("return.return") %><br>
                            <%= f.input :clean %>
                          </div>

                        </div>

                        <div class="row text-center">                        

                          <div class="col-md-4">
                            <%= t("return.control") %><br>
                            <%= f.input :dirty_ctrl %>
                          </div>

                          <div class="col-md-4">
                            <%= t("return.control") %><br>
                            <%= f.input :sealed_ctrl %>
                          </div>

                          <div class="col-md-4">
                            <%= t("return.control") %><br>
                            <%= f.input :clean_ctrl %>
                          </div>

                        </div>
                      </div>

                      <div class="modal-footer">

                        <button type="button" class="btn btn-default" data-dismiss="modal">
                          <%= t("menu.close") %>
                        </button>
                
                          <%= f.button :submit, class: "btn btn-info" %>
          
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
        <% end %>
      </li>
    </ul>

