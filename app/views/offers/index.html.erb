<div class="container-fluid more-padding">

	<% if current_user.admin %>

		<%= render 'index_admin' %>

	<% elsif current_user.client %>

		<%= render 'index_client' %>

	<% end %>

</div> <!-- container-fluid -->
