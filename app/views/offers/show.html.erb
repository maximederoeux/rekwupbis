<%= Box.is_rekwup.is_twenty.closed.first.box_name %>

	<% if current_user.admin %>
	    
		<%= render 'event_info_admin' %>

		<%= render 'box_detail' %>

		<%= render 'add_box' %>

		<%= render 'article_detail' %>

		<%= render 'add_article' %>

	<% end %>

	<% if current_user.client && current_user == @organizer %>

		<%= render 'event_info_client' %>

		<%= render 'box_detail' %>

		<%= render 'article_detail' %>

	<% end %>






